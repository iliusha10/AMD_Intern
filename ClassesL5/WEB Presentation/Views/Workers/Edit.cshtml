@model WEB_Presentation.Models.PersonModel

@{
    ViewBag.Title = "Edit";
}

<script type="text/javascript">
    $("#EditSubmitButton").click(function () {
        document.location = '@Url.Action("EditIntern", "Workers")';
    })
</script>

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>Worker</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.EditorForModel()
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                @*<input type="submit" value="Save" class="btn btn-default" id="EditSubmitButton">*@
                <button >@Url.Action("EditIntern", "Workers")</button>
                
            </div>
        </div>
    </div>
}



<div>
    @Html.ActionLink("Back to List", "Index")
</div>

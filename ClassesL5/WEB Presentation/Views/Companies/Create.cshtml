@model WEB_Presentation.Models.CompanyModel

@{
    ViewBag.Title = "Create2";
}

<h2>Create2</h2>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>CompanyModel</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.EditorFor(model => model.CompanyName);

        <div class="form-group">
            @Html.LabelFor(model => model.Activity, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @*@Html.DropDownList("FieldOfActivity", EnumHelper.GetSelectList(typeof(FieldOfActivity)) , "Select field of activity", new { @class = "form-control" })*@
                @Html.EnumDropDownListFor(model => model.Activity, new { @class = "form-control" })
                @Html.ValidationMessageFor(model => model.Activity, "", new { @class = "text-danger" })
            </div>
        </div>

        @Html.EditorFor(model => model.City);
        @Html.EditorFor(model => model.Street);

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
